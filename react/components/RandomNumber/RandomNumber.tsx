import React from 'react';
import styles from './styles.css';

const RandomNumber = () => {

    return (
        <>
            <h2 className={ `vtex-rich-text-0-x-paragraph--randomNumber ${styles.textRandomNumber}`}>
                Prueba
            </h2>
            <p className={ `vtex-rich-text-0-x-paragraph--randomNumber ${styles.textRandomNumber}` }>
                No lo pierdas!!! tenemos 17 personas insteresadas en este producto
            </p>
        </>
        

    )
}

export default RandomNumber;