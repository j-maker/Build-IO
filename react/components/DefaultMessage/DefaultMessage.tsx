import React from 'react';
import styles from './styles.css';

const DefaultMessage = () => {

    return (
        <h2 className={ `vtex-rich-text-0-x-paragraph--customjd ${styles.textDefaultMessage}` }>
            Este es mi mensaje 2
        </h2>

    )
}

export default DefaultMessage;